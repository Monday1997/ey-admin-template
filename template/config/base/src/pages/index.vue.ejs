<template>
  <div
    class="min-h-screen flex justify-center items-center bg-gradient-to-r from-blue-100 via-purple-100 to-pink-100"
  >
    <div
      class="backdrop-blur-md bg-white/60 rounded-2xl shadow-lg p-10 w-full max-w-[900px] flex flex-col justify-center items-center text-center border border-white/30"
    >
      <%- config.includes('i-18n') && `
      <!-- 标题 -->
      <h1 class="text-3xl font-bold text-gray-800 mb-8 drop-shadow-sm">
        {{ $t('message.hello') }}
      </h1>

      <!-- 语言选择 -->
      <div class="flex flex-col items-center gap-4 mb-10">
        <label class="text-gray-700 font-medium">🌐 选择语言 - i18n</label>
        <select
          v-model="locale"
          class="w-44 px-3 py-2 rounded-lg border border-gray-300 bg-white/80 text-gray-700 focus:outline-none focus:ring-2 focus:ring-blue-400 transition"
        >
          <option value="en">English</option>
          <option value="zh-CN">简体中文</option>
        </select>
      </div>
      ` %>

      <!-- 项目提示 -->
      <div
        class="bg-gradient-to-r from-blue-50 to-purple-50 border border-blue-200 rounded-xl p-6 w-full max-w-md text-center shadow-sm"
      >
        <h2 class="text-lg font-semibold text-gray-800 mb-3">
          🎉✨ 当前项目已集成以下特性 ✨🎉
        </h2>
        <ul class="text-gray-700 mb-4 space-y-1 font-medium">
          <%- 
          config
          .concat(css==='unocss' ? 'unocss' : 'tailwind')
          .concat(axios?'axios':[])
          .concat(['Ant Design Vue','ey-admin',])
          .filter(item=>item!=='base')
          .map(item => `<li>✅ ${item}</li>`)
          .join('') %>
        </ul>
        <p class="text-gray-700">
          项目细节可参考
          <a
            href="https://www.monday1997.cn"
            target="_blank"
            class="text-blue-600 font-semibold hover:underline"
          >
            官网
          </a>
        </p>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
  import { ref, watch } from 'vue'

  import { loadLocaleMessages } from '@/modules/i18n'

  const locale = ref('zh-CN')
  watch(locale, (newLocale) => {
    loadLocaleMessages(newLocale)
  })

  const route = useRoute()
  console.log('🚀 ~ route:', route)
  console.log('🚀 ~ route:', route.meta?.name)
</script>
<%- config.includes('router') ? `<route lang="json">
  { "name": "index", "meta": { "name": "test-name" } } </route
>` :'' %>
